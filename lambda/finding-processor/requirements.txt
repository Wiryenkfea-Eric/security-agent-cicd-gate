# No external dependencies — uses Python 3.12 standard library only:
# json, boto3 (provided by Lambda runtime), urllib.request, base64
# boto3 is pre-installed in all Lambda Python runtimes — do not include in zip
